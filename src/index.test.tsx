import * as React from 'react';
import {render} from 'react-dom';
import {AudioProvider} from './index';

const container = document.getElementById('container');

render(<AudioProvider>

</AudioProvider>, container);